# CreditGuard

To run this, you would need to first download the dataset from Kaggle (https://www.kaggle.com/datasets/kartik2112/fraud-detection/data) and unzip the file.
Afterwards, create a directory/folder named "dataset" and put the unzipped files into that directory/folder.

Within the notebook, run each cell and ensure the cuda is enabled to run this.
